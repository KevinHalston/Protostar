; 1                                      .join()
; join() trong python là phương thức dùng để nối chuỗi trong Python.

; Chúng ta sử dụng phương thức join() trong python để nối các chuỗi ký tự là phần tử trong một list hoặc tuple lại với nhau, bằng một hoặc một chuỗi ký tự phân cách.

; Cú pháp phương thức join() trong Python
; Cú pháp join() trong python như sau:

; str.join(iterable)

; Trong đó

; str là ký tự phân cách dùng để nối chuỗi
; join là tên phương thức
; iterable là list hoặc tuple chứa phần tử là các chuỗi cần nối.
; Tham khảo chi tiết về iterable tại bài viết iterable là gì.
; Kết quả thu được sẽ là một chuỗi ký tự gồm các phần tử trong list hoặc tuple đã chỉ định, được nối lại với nhau bằng ký tự phân cách.

; Chương trình mẫu sử dụng phương thức join() trong Python

; str = ", "
; iterable = ("Java", "Python", "PHP")
; print (str.join(iterable))

; Kết quả
; Java, Python, PHP

; 2.                  dict()
; Sử dụng như từ điển ví dụ:
;         str_so = dict()
;         n = int(input("Nhap so muon tinh : "))
;         for i in range (1, n+1):
;             str_so[i] = i * i
;         print(str_so)
; Ví dụ: Giả sử số n là 8 thì đầu ra sẽ là: {1: 1, 2: 4, 3: 9, 4: 16, 5: 25, 6: 36, 7: 49, 8: 64}.

